export enum ButtonStatus {
	INACTIVE = 'unchecked',
	ACTIVE = 'checked',
}

export enum Keys {
	PRESCRIPTIONS = '@prescriptions',
	LOGS = '@logs',
}

export enum Routes {
	HOME = 'Home',
	PRESCRIPTIONS = 'Prescriptions',
	LOGS = 'Logs',
}

export enum RouteIcons {
	HOME = 'home',
	PRESCRIPTIONS = 'prescription',
	LOGS = 'file-document',
}
